rm(list=ls())

library(dplyr)
library(tidyr)
library(INLA)
library(spdep)
library(lubridate)
library(stringr)
library(purrr)
library(zoo)
library(tibble)
library(tsibble)
library(Matrix) 

# load data

# calculate total

# define prior

# build adjancency matrix from shapefile

# run inla model (just for cases)